import React from 'react'
import { SiSetapp } from 'react-icons/si'

const AppIcon = () => {
    return (
        <div className={`file text-blue-400 `}>
            <SiSetapp size={70} className={`file text-blue-400 pointer-events-none `} />
        </div>
    )
}

export default AppIcon